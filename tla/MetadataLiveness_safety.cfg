\* MetadataLiveness - Safety sanity check (no fairness, just invariants)
\* Verifies the base model still works with request tracking added

SPECIFICATION Spec

CONSTANTS
    NUM_STRIPES = 2
    MAX_CRASHES = 1
    MAX_RETRIES = 2
    NUM_REQUESTS = 2

INVARIANTS
    TypeOK
    DurabilityBeforeVisibility
    NoStaleReadsAfterCrash
